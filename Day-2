1)class Solution(object):
    def countDigitOccurrences(self, nums, digit):
        a=0
        b=[]
        digit=str(digit)
        for i in nums:
            i=str(i)
            for j in i:
                if digit==j:
                    a+=1
        return a
